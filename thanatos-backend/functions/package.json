{
  "name": "thanatos-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test:lore": "tsx src/services/aiLoreService.ts"
  },
  "dependencies": {
    "@openzeppelin/merkle-tree": "^1.0.8",
    "dotenv": "^17.4.2",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "openai": "^4.73.0",
    "twitter-api-v2": "^1.29.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
